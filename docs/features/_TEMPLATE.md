# (화면 이름)

> (이 화면이 무엇을 하는 곳인지 한 문장. 사용자 입장에서 씁니다. "병원 한 곳의 정보를 자세히 보고 상담을 신청하는 화면입니다." 처럼)

| | |
|---|---|
| **주소** | `/경로` |
| **누가 보나요** | 일반 사용자 / 로그인한 사용자 / 병원 관리자 |
| **어디서 들어오나요** | (이 화면으로 오는 경로) |
| **소스 파일** | `frontend/src/...` |

---

## 여기서 할 수 있는 일

상태 표시: ✅ 실제로 동작함 · 🟡 화면은 되지만 데이터가 샘플 · 🚧 아직 아무 일도 안 일어남 · 🔒 로그인 필요

| 할 수 있는 일 | 상태 | 지금 어떻게 동작하나요 |
|---|---|---|
| (예: 상담 신청하기) | ✅ 🔒 | (눌렀을 때 실제로 무슨 일이 일어나는지. 저장되는지, 어디로 가는지) |

---

## 화면에 보이는 정보

- **(영역 이름)** — (무엇이 보이는지). (없을 때는 어떻게 되는지도 적어주세요)

---

## 이 화면에서 이동할 수 있는 곳

| 어디로 | 무엇을 눌렀을 때 |
|---|---|

---

## 알아두실 것

(비개발자가 "이거 고장난 것 같다"고 오해할 수 있는 지점을 미리 적습니다. 예:)

- (샘플 데이터라서 실제 병원이 아닙니다)
- (지금은 저장만 되고 실제로 병원에 연락이 가지는 않습니다)
- (특정 조건에서만 보이는 영역)

---

## 이 화면을 바꾸고 싶을 때

`docs/features/requests/_TEMPLATE.md` 를 복사해서 `docs/features/requests/` 에 요청서를 만들거나,
채팅창에 `/feature-request` 를 입력하세요.

이 화면에서 손대면 함께 확인해야 하는 것:

- (예: 상담 신청 폼과 관리자 상담 목록이 같은 데이터를 씁니다)
