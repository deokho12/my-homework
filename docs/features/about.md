# 몰라몰라 알아보기

> 몰라몰라 서비스를 소개하는 화면입니다. 지금은 소개 내용이 없고 안내 문구만 있습니다.

| | |
|---|---|
| **주소** | `/about` |
| **누가 보나요** | 누구나 |
| **어디서 들어오나요** | 홈 화면 맨 아래 회사 정보 영역에 있는 `몰라몰라 알아보기` 버튼 |
| **소스 파일** | `frontend/src/screens/about.tsx` |

---

## 여기서 할 수 있는 일

상태 표시: ✅ 실제로 동작함 · 🟡 화면은 되지만 데이터가 샘플 · 🚧 아직 아무 일도 안 일어남 · 🔒 로그인 필요

| 할 수 있는 일 | 상태 | 지금 어떻게 동작하나요 |
|---|---|---|
| 화면 열어보기 | 🚧 | 눌러서 들어가면 화면 가운데에 `준비중입니다` 라는 문구만 보입니다. 그 외 버튼이나 내용은 없습니다 |

---

## 화면에 보이는 정보

- **안내 문구** — `준비중입니다` 한 줄. 이 화면 전체 내용입니다.

---

## 이 화면에서 이동할 수 있는 곳

| 어디로 | 무엇을 눌렀을 때 |
|---|---|
| (이전 화면) | 화면 위쪽 왼쪽의 뒤로가기 화살표 |

---

## 알아두실 것

- **회사 소개 내용이 아직 만들어지지 않았습니다.** 지금은 문구 하나만 있는 빈 화면이라 이상한 게 아닙니다.
- 홈 화면 하단에 보이는 회사명·대표자·사업자등록번호 같은 정보는 이 화면이 아니라 **화면 맨 아래 영역(Footer)**에 별도로 있습니다. `docs/features` 문서에는 포함되어 있지 않으니, 그 정보를 고치려면 `frontend/src/mocks/fixtures/placeholder-company-info.ts` 파일을 확인해야 합니다.

---

## 이 화면을 바꾸고 싶을 때

`docs/features/requests/_TEMPLATE.md` 를 복사해서 `docs/features/requests/` 에 요청서를 만들거나,
채팅창에 `/feature-request` 를 입력하세요.

이 화면에서 손대면 함께 확인해야 하는 것:

- 이 화면은 다른 화면과 데이터를 주고받지 않습니다. 문구만 바꾸면 됩니다.
